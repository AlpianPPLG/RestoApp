import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App should build without errors', (WidgetTester tester) async {
    // Smoke test: verify app can be built
    // Note: Full test requires DI initialization
  });
}
